[Unit]
Description=Hourly Mailyte incremental backup

[Timer]
# :15 past, so it never collides with the 02:30 full backup.
OnCalendar=*-*-* *:15:00
Persistent=true
AccuracySec=1min
Unit=mailyte-backup-incremental.service

[Install]
WantedBy=timers.target
