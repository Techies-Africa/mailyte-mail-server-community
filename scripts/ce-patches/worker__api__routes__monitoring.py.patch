--- a/worker/api/routes/monitoring.py
+++ b/worker/api/routes/monitoring.py
@@ -1,75 +1,76 @@
 #!/usr/bin/env python3
 """
-Monitoring API Routes - Version 1.0
+Monitoring API Routes
 
-Provides versioned API endpoints for monitoring services with organization support:
-- Health checks and service status across the organization hierarchy
-- System metrics and performance data with multi-tenant awareness
-- Enterprise metrics collection for organizations, domains, and email accounts
-- Administrative monitoring controls with organization-level access
+Service health, per-service status, system metrics, dashboard stats, restart,
+and auto-heal -- the Infrastructure section of the Mailyte Console (ADR-004).
 
-This module acts as an abstraction layer over the internal monitoring service,
-exposing organization-aware monitoring capabilities while maintaining backward
-compatibility with existing monitoring integrations.
+This module is a thin, authenticated proxy over a monitoring service. It owns
+no state of its own; it exists so the console has one authenticated surface
+instead of needing network reach to internal containers.
 
-Key Features:
-- Organization hierarchy monitoring and alerting
-- Domain-level health and performance tracking
-- Email account activity monitoring
-- Backward compatibility with existing monitoring endpoints
-- Enhanced security and access control for multi-tenant environments
+CE PACKAGING NOTE -- read before filing a bug about a 503 here. This repo's
+docker-compose.yml ships no monitoring container (mailyte-email-server has
+one; CE does not). Every route below therefore returns 503 with an explicit
+message unless MONITORING_SERVICE_URL is set to something that exists. That
+check runs FIRST, before the admin-token check on the mutating routes, so an
+operator gets "there is no monitoring service configured" rather than a
+misleading "admin token required". There is deliberately no default URL: the
+value mailyte-email-server defaults to (http://0.0.0.0:8080) is this API's
+own bind port inside the container, so a default would make /health proxy to
+itself and report nonsense instead of failing honestly.
+
+Scope: every route here is scope='platform' -- a tenant credential can never
+reach it regardless of its own permission flags, because none of these routes
+have an organization concept at all (they describe infrastructure, not
+tenants). Reads require role='support'; the destructive POSTs require
+role='operator', because ADR-002 §4 makes "restart services" an operator
+capability and explicitly denies it to support.
 """
 
 import os
 from datetime import datetime
 
 import requests
+
+# requests is blocking. In an `async def` handler it runs ON the event loop and
+# stalls every other request this worker is serving. Measured on
+# mailyte-email-server before the same fix: 10.3s for a call the monitoring
+# service answered in 0.18s. Latent here only because CE ships no monitoring
+# container -- but docker-compose.yml invites operators to set
+# MONITORING_SERVICE_URL, and the moment they do, this bites.
 from fastapi import APIRouter, Request
 from fastapi.responses import JSONResponse
 from pydantic import BaseModel, Field
-from schemas.monitoring import MonitoringErrorResponse, MonitoringProxyResponse
 from starlette.concurrency import run_in_threadpool
 from utils.auth import require_api_key
 
-# All 8 routes in this file require a guard -- none of them are the public
-# healthcheck. The container healthcheck Docker/uptime probes hit is the
-# root-level `GET /health` defined in app.py, not `/api/v1/monitoring/health`
-# below (which returns internal component status and is tenant-facing data).
-#
-# The 3 POST routes below (restart_service, trigger_auto_heal, test_webhooks)
-# previously checked only that an `X-Admin-Token` header was *present* --
-# never validated it against anything -- before forwarding it to the internal
-# monitoring service, which does the real check. That left the API gateway
-# itself with no independent authentication for the three most destructive
-# operations in the whole surface (deep-audit.md §2.1 calls these
-# "restarting infrastructure, cross-tenant blast radius"). They now also
-# require `require_api_key('admin')` at the gateway; the X-Admin-Token
-# forwarding is unchanged because the downstream monitoring service still
-# needs it for its own check.
-#
-# Phase-06 (ADR-002): every route in this file is scope='platform' -- a
-# tenant credential can never reach it regardless of its own permission
-# flags, since deep-audit.md §2.1 classifies the whole file as
-# unambiguously platform-only (proxies to internal infra with no
-# organization concept at all, confirmed live: none of the 8 routes filter
-# by organization_id anywhere). Read routes require role='support' (view
-# health/services/metrics -- ADR-002 §4); the 3 destructive POSTs require
-# role='operator' ("restart services" is explicitly an operator capability).
-# Import updated database models with organization hierarchy support
-# These models enable organization-aware monitoring and metrics collection
-
-# Create monitoring router
 router = APIRouter()
 
-# Internal monitoring service configuration
-# 0.0.0.0 is a BIND address, never a CONNECT address. Inside the api
-# container it resolves to the api itself, so this gateway spent every
-# request calling its own port and timing out -- it has never worked under
-# Docker. The service name is what one container calls another by.
-# monitoring binds 8085 (docker-compose + its own uvicorn --port).
-MONITORING_SERVICE_URL = os.getenv("MONITORING_SERVICE_URL", "http://monitoring:8085")
+# No default -- see the CE packaging note above.
+MONITORING_SERVICE_URL = os.getenv("MONITORING_SERVICE_URL", "").strip()
 
+_NO_SERVICE_MESSAGE = (
+    "No monitoring service is configured on this deployment. The Community Edition "
+    "compose file ships no monitoring container; set MONITORING_SERVICE_URL on the api "
+    "service to point at one to enable these endpoints."
+)
 
+
+def _unavailable() -> JSONResponse:
+    """503, not 501: the endpoint exists and its contract is real -- there is
+    simply nothing behind it on this deployment. The console renders an
+    'unavailable' state from this rather than treating it as a bug."""
+    return JSONResponse(
+        content={
+            "type": "error",
+            "msg": _NO_SERVICE_MESSAGE,
+            "timestamp": datetime.utcnow().isoformat() + "Z",
+        },
+        status_code=503,
+    )
+
+
 # ---------------------------------------------------------------------------
 # Request Models
 # ---------------------------------------------------------------------------
@@ -80,7 +81,8 @@
 
     force: bool | None = Field(
         False,
-        description="If true, force-kill the service before restarting instead of a graceful restart",
+        description="If true, force-kill the service before restarting instead of a "
+        "graceful restart",
     )
     reason: str | None = Field(
         None, description="Human-readable reason for the restart, recorded in the audit log"
@@ -92,7 +94,8 @@
 
     services: list | None = Field(
         None,
-        description="Limit auto-heal to specific services by name. Omit to heal all unhealthy services.",
+        description="Limit auto-heal to specific services by name. Omit to heal all "
+        "unhealthy services.",
     )
     dry_run: bool | None = Field(
         False, description="If true, report which services would be healed without taking action"
@@ -118,27 +121,16 @@
 @router.get(
     "/health",
     summary="System health check",
-    description="Returns the overall health of all email server components including Postfix, Dovecot, Rspamd, MySQL, and Redis. Any component failure is reported with error details.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        503: {
-            "model": MonitoringErrorResponse,
-            "description": "Monitoring service returned a non-200 status",
-        },
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        },
-    },
+    description="Returns the overall health of all email server components including Postfix, "
+    "Dovecot, Rspamd, MySQL, and Redis. Any component failure is reported with error "
+    "details. NOT the container healthcheck -- that is the unauthenticated GET /health "
+    "in app.py. 503 when this deployment has no monitoring service.",
 )
 @require_api_key("read", scope="platform", role="support")
 async def health_check():
-    """
-    Get overall monitoring service health status.
-
-    Returns:
-        JSON response with monitoring service health
-    """
+    """Get overall monitoring service health status."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
     try:
         response = await run_in_threadpool(
             lambda: requests.get(f"{MONITORING_SERVICE_URL}/health", timeout=10)
@@ -149,15 +141,14 @@
                 "monitoring_service": response.json(),
                 "timestamp": datetime.utcnow().isoformat() + "Z",
             }
-        else:
-            return JSONResponse(
-                content={
-                    "status": "unhealthy",
-                    "error": f"Monitoring service returned {response.status_code}",
-                    "timestamp": datetime.utcnow().isoformat() + "Z",
-                },
-                status_code=503,
-            )
+        return JSONResponse(
+            content={
+                "status": "unhealthy",
+                "error": f"Monitoring service returned {response.status_code}",
+                "timestamp": datetime.utcnow().isoformat() + "Z",
+            },
+            status_code=503,
+        )
     except Exception as e:
         return JSONResponse(
             content={
@@ -165,88 +156,67 @@
                 "error": str(e),
                 "timestamp": datetime.utcnow().isoformat() + "Z",
             },
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.get(
     "/services",
     summary="Get all services status",
-    description="Returns the current status of every monitored service including Postfix, Dovecot, Rspamd, ClamAV, MySQL, and Redis. Each service entry includes uptime, last heartbeat, and any active alerts.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        }
-    },
+    description="Returns the current status of every monitored service including Postfix, "
+    "Dovecot, Rspamd, MySQL, and Redis. Each service entry includes uptime, last "
+    "heartbeat, and any active alerts.",
 )
 @require_api_key("read", scope="platform", role="support")
 async def get_services_status():
-    """
-    Get status of all monitored services.
-
-    Returns:
-        JSON response with all service statuses
-    """
+    """Get status of all monitored services."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
     try:
         response = await run_in_threadpool(
             lambda: requests.get(f"{MONITORING_SERVICE_URL}/heartbeat", timeout=15)
         )
         if response.status_code == 200:
             return response.json()
-        else:
-            return JSONResponse(
-                content={
-                    "error": "Failed to get services status",
-                    "status_code": response.status_code,
-                },
-                status_code=response.status_code,
-            )
+        return JSONResponse(
+            content={
+                "error": "Failed to get services status",
+                "status_code": response.status_code,
+            },
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={"error": str(e), "timestamp": datetime.utcnow().isoformat() + "Z"},
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.get(
     "/services/{service_name}",
     summary="Get single service status",
-    description="Returns the health and performance details for a specific monitored service. Includes process status, resource consumption, last heartbeat time, and recent error history.",
+    description="Returns the health and performance details for a specific monitored service. "
+    "Includes process status, resource consumption, last heartbeat time, and recent "
+    "error history.",
 )
 @require_api_key("read", scope="platform", role="support")
 async def get_service_status(service_name: str):
-    """
-    Get status of a specific service.
-
-    Args:
-        service_name: Name of the service to check
-
-    Returns:
-        JSON response with service status
-    """
+    """Get status of a specific service."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
     try:
-        # Threadpooled like its siblings -- this one is a multi-line call and
-        # was missed on the first pass, which left exactly one blocking call on
-        # the event loop and this route timing out while the others were fine.
-        response = await run_in_threadpool(
-            lambda: requests.get(
-                f"{MONITORING_SERVICE_URL}/heartbeat",
-                params={"service": service_name},
-                timeout=10,
-            )
-        )
+        response = await run_in_threadpool(lambda: requests.get(
+            f"{MONITORING_SERVICE_URL}/heartbeat", params={"service": service_name}, timeout=10
+        ))
         if response.status_code == 200:
             return response.json()
-        else:
-            return JSONResponse(
-                content={
-                    "error": f"Failed to get status for service {service_name}",
-                    "status_code": response.status_code,
-                },
-                status_code=response.status_code,
-            )
+        return JSONResponse(
+            content={
+                "error": f"Failed to get status for service {service_name}",
+                "status_code": response.status_code,
+            },
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={
@@ -254,111 +224,87 @@
                 "service": service_name,
                 "timestamp": datetime.utcnow().isoformat() + "Z",
             },
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.get(
     "/metrics",
     summary="Get system metrics",
-    description="Returns current system and mail server metrics including CPU, memory, disk usage, mail throughput rates, queue depths, and connection counts. Suitable for feeding into external monitoring dashboards.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        }
-    },
+    description="Returns current system and mail server metrics including CPU, memory, disk "
+    "usage, mail throughput rates, queue depths, and connection counts. Suitable for "
+    "feeding into external monitoring dashboards.",
 )
 @require_api_key("read", scope="platform", role="support")
 async def get_system_metrics():
-    """
-    Get current system and mail server metrics.
-
-    Returns:
-        JSON response with comprehensive metrics
-    """
+    """Get current system and mail server metrics."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
     try:
         response = await run_in_threadpool(
             lambda: requests.get(f"{MONITORING_SERVICE_URL}/api/metrics", timeout=15)
         )
         if response.status_code == 200:
             return response.json()
-        else:
-            return JSONResponse(
-                content={
-                    "error": "Failed to get system metrics",
-                    "status_code": response.status_code,
-                },
-                status_code=response.status_code,
-            )
+        return JSONResponse(
+            content={"error": "Failed to get system metrics", "status_code": response.status_code},
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={"error": str(e), "timestamp": datetime.utcnow().isoformat() + "Z"},
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.get(
     "/stats",
     summary="Get dashboard statistics",
-    description="Returns pre-aggregated statistics optimised for monitoring dashboards. Includes mail volume trends, delivery success rates, spam ratios, and storage utilisation summaries over configurable time windows.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        }
-    },
+    description="Returns pre-aggregated statistics optimised for monitoring dashboards. Includes "
+    "mail volume trends, delivery success rates, spam ratios, and storage utilisation "
+    "summaries over configurable time windows.",
 )
 @require_api_key("read", scope="platform", role="support")
 async def get_dashboard_stats():
-    """
-    Get dashboard statistics for monitoring display.
-
-    Returns:
-        JSON response with dashboard-friendly stats
-    """
+    """Get dashboard statistics for monitoring display."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
     try:
         response = await run_in_threadpool(
             lambda: requests.get(f"{MONITORING_SERVICE_URL}/api/stats", timeout=10)
         )
         if response.status_code == 200:
             return response.json()
-        else:
-            return JSONResponse(
-                content={
-                    "error": "Failed to get dashboard stats",
-                    "status_code": response.status_code,
-                },
-                status_code=response.status_code,
-            )
+        return JSONResponse(
+            content={"error": "Failed to get dashboard stats", "status_code": response.status_code},
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={"error": str(e), "timestamp": datetime.utcnow().isoformat() + "Z"},
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.post(
     "/services/{service_name}/restart",
     summary="Restart a service",
-    description="Initiates a restart of the specified email server component. Requires an admin-scoped API key plus admin authentication via X-Admin-Token header. Supports graceful and forced restart modes. The operation is logged in the audit trail.",
+    description="Initiates a restart of the specified email server component. Requires an "
+    "operator-role platform credential, plus the downstream monitoring service's own "
+    "X-Admin-Token, which is forwarded unchanged. Supports graceful and forced restart "
+    "modes. The operation is written to operator_audit by middleware.",
 )
 @require_api_key("admin", scope="platform", role="operator")
 async def restart_service(
     service_name: str, request: Request, body: ServiceRestartRequest | None = None
 ):
-    """
-    Restart a specific service (requires admin authentication).
+    """Restart a specific service."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
 
-    Args:
-        service_name: Name of the service to restart
-
-    Returns:
-        JSON response with restart operation result
-    """
-    # Forward admin token from request headers
+    # Forwarded, not validated here: this gateway has already authenticated
+    # and role-checked the caller; the header exists because the downstream
+    # monitoring service performs its own independent check.
     admin_token = request.headers.get("X-Admin-Token")
     if not admin_token:
         return JSONResponse(
@@ -371,32 +317,28 @@
 
     try:
         headers = {"X-Admin-Token": admin_token}
-        response = await run_in_threadpool(
-            lambda: requests.post(
-                f"{MONITORING_SERVICE_URL}/restart/{service_name}", headers=headers, timeout=30
-            )
-        )
+        response = await run_in_threadpool(lambda: requests.post(
+            f"{MONITORING_SERVICE_URL}/restart/{service_name}", headers=headers, timeout=30
+        ))
 
         if response.status_code == 200:
             return response.json()
-        elif response.status_code == 401:
+        if response.status_code == 401:
             return JSONResponse(
                 content={
                     "error": "Invalid admin token",
                     "timestamp": datetime.utcnow().isoformat() + "Z",
                 },
                 status_code=401,
-            )
-        else:
-            return JSONResponse(
-                content={
-                    "error": f"Failed to restart service {service_name}",
-                    "status_code": response.status_code,
-                    "response": response.text,
-                },
-                status_code=response.status_code,
             )
-
+        return JSONResponse(
+            content={
+                "error": f"Failed to restart service {service_name}",
+                "status_code": response.status_code,
+                "response": response.text,
+            },
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={
@@ -404,35 +346,24 @@
                 "service": service_name,
                 "timestamp": datetime.utcnow().isoformat() + "Z",
             },
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.post(
     "/auto-heal",
     summary="Trigger auto-heal for all services",
-    description="Runs the auto-healing procedure across all monitored services. Detects unhealthy components and attempts automatic recovery (restart, config reload, cache flush). Requires an admin-scoped API key plus admin authentication via X-Admin-Token header. Supports dry-run mode.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        401: {
-            "model": MonitoringErrorResponse,
-            "description": "Admin token (X-Admin-Token) missing or invalid",
-        },
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        },
-    },
+    description="Runs the auto-healing procedure across all monitored services. Detects unhealthy "
+    "components and attempts automatic recovery (restart, config reload, cache flush). "
+    "Requires an operator-role platform credential plus the downstream service's "
+    "X-Admin-Token. Supports dry-run mode.",
 )
 @require_api_key("admin", scope="platform", role="operator")
 async def trigger_auto_heal(request: Request, body: AutoHealRequest | None = None):
-    """
-    Trigger auto-healing for all services (requires admin authentication).
+    """Trigger auto-healing for all services."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
 
-    Returns:
-        JSON response with auto-heal operation results
-    """
-    # Forward admin token from request headers
     admin_token = request.headers.get("X-Admin-Token")
     if not admin_token:
         return JSONResponse(
@@ -453,7 +384,7 @@
 
         if response.status_code == 200:
             return response.json()
-        elif response.status_code == 401:
+        if response.status_code == 401:
             return JSONResponse(
                 content={
                     "error": "Invalid admin token",
@@ -461,48 +392,35 @@
                 },
                 status_code=401,
             )
-        else:
-            return JSONResponse(
-                content={
-                    "error": "Failed to trigger auto-heal",
-                    "status_code": response.status_code,
-                    "response": response.text,
-                },
-                status_code=response.status_code,
-            )
-
+        return JSONResponse(
+            content={
+                "error": "Failed to trigger auto-heal",
+                "status_code": response.status_code,
+                "response": response.text,
+            },
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={"error": str(e), "timestamp": datetime.utcnow().isoformat() + "Z"},
-            status_code=500,
+            status_code=503,
         )
 
 
 @router.post(
     "/webhooks/test",
     summary="Test webhook endpoints",
-    description="Sends a test payload to all configured webhook endpoints (or a specified subset) to verify connectivity and correct response handling. Requires an admin-scoped API key plus admin authentication via X-Admin-Token header. Returns per-webhook delivery status and response times.",
-    response_model=MonitoringProxyResponse,
-    responses={
-        401: {
-            "model": MonitoringErrorResponse,
-            "description": "Admin token (X-Admin-Token) missing or invalid",
-        },
-        500: {
-            "model": MonitoringErrorResponse,
-            "description": "Unhandled exception contacting the monitoring service",
-        },
-    },
+    description="Sends a test payload to all configured webhook endpoints (or a specified subset) "
+    "to verify connectivity and correct response handling. Requires an operator-role "
+    "platform credential plus the downstream service's X-Admin-Token. Returns per-webhook "
+    "delivery status and response times.",
 )
 @require_api_key("admin", scope="platform", role="operator")
 async def test_webhooks(request: Request, body: WebhookTestRequest | None = None):
-    """
-    Test all configured webhook endpoints (requires admin authentication).
+    """Test all configured webhook endpoints."""
+    if not MONITORING_SERVICE_URL:
+        return _unavailable()
 
-    Returns:
-        JSON response with webhook test results
-    """
-    # Forward admin token from request headers
     admin_token = request.headers.get("X-Admin-Token")
     if not admin_token:
         return JSONResponse(
@@ -515,15 +433,13 @@
 
     try:
         headers = {"X-Admin-Token": admin_token}
-        response = await run_in_threadpool(
-            lambda: requests.post(
-                f"{MONITORING_SERVICE_URL}/test/webhooks", headers=headers, timeout=30
-            )
-        )
+        response = await run_in_threadpool(lambda: requests.post(
+            f"{MONITORING_SERVICE_URL}/test/webhooks", headers=headers, timeout=30
+        ))
 
         if response.status_code == 200:
             return response.json()
-        elif response.status_code == 401:
+        if response.status_code == 401:
             return JSONResponse(
                 content={
                     "error": "Invalid admin token",
@@ -531,18 +447,16 @@
                 },
                 status_code=401,
             )
-        else:
-            return JSONResponse(
-                content={
-                    "error": "Failed to test webhooks",
-                    "status_code": response.status_code,
-                    "response": response.text,
-                },
-                status_code=response.status_code,
-            )
-
+        return JSONResponse(
+            content={
+                "error": "Failed to test webhooks",
+                "status_code": response.status_code,
+                "response": response.text,
+            },
+            status_code=response.status_code,
+        )
     except Exception as e:
         return JSONResponse(
             content={"error": str(e), "timestamp": datetime.utcnow().isoformat() + "Z"},
-            status_code=500,
+            status_code=503,
         )
