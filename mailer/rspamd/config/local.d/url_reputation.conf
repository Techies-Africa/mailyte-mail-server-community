# =============================================================================
# Rspamd URL Reputation — Track and score URLs based on historical behavior
# =============================================================================

enabled = true;

# Redis backend for URL reputation data
servers = "redis:6379";

# Expire URL reputation data after 30 days
expire = 2592000;

# Scoring thresholds
threshold = 1;
