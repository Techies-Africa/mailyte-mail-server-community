# Direct dependencies of app.py plus the lazily-imported ones inside
# shared/webhook_dispatcher.py (requests for delivery, mysql.connector for its
# delivery log and DLQ writes). shared/'s own package list is installed
# separately from shared/requirements.txt -- see the note in that file about
# totp/dlp/geo_blocking crash-looping when an inline pip list drifted from the
# shared code the image had baked in.
mysql-connector-python==8.3.0
requests==2.32.3
